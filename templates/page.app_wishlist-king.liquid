{% comment %} // Uncomment if option_selection.js is missing
{{ 'option_selection.js' | shopify_asset_url | script_tag }}
{% endcomment %}

<div id="wishlist-king" class="wk-row">
  <div class="wk-span12">

    <h1 id="wk-page-title">{{ page.title }}</h1>

    {{ page.content }}

    {%- unless page.handle contains 'share' -%}
    <!-- include 'wishlist-collection' -->
    {%- else -%}
    <!-- include 'wishlist-shared' -->
    {%- endunless -%}

  </div>

</div>
